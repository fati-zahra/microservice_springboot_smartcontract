server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fati-zahra/smart_contract_sts.git
          
          clone-on-start: true